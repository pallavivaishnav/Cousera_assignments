def computepay(h,r):
    if(h>40):
        reg=h*r
        otp=(h-40.0)*(r*0.5)
        op=reg+otp
    else:
        op=h*r
    return op

hrs = float(input("Enter Hours:"))
rate=float(input("Enter rate"))
p = computepay(hrs,rate)
print("Pay",p)
